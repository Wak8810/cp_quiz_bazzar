<header>
  <div class="header">
    <%= link_to 'BUZZAR', root_path, class: 'logo' %>
    <nav>
      <% if current_user %>
        <%= link_to 'クイズを投稿', new_user_quiz_path(current_user) %>
        <%= link_to 'クイズを閲覧', user_quizzes_path(current_user) %>
        <button class=nav-link>回答履歴</button>
      <% end %>
      <% if !current_user %>
        <button class=nav-link>ログイン</button>
        <button class=nav-link>登録</button>
      <% end %>
    </nav>
  </div>
</header>
